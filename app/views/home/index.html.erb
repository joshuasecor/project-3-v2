
<div class="hero">
    <img src="assets/CarouselTop_02.png">
</div>

<div class="container">
	<h1></h1>

	<br><br>
	<hr>
	<div class="featuredServices">
		<% @services.where(is_featured: 'true').each do |s| %>
		<div>
      <%= image_tag s.photo.url(:thumb) %>
			<%= link_to s.name, s %><br><br>
			<%= s.description %>
		</div>
		<br>
		<% end %>
	</div>
	<br>
	<hr>
	<div class="about_text">
		ABOUT US: <br><br>
		<%= @about.about_text %>
	</div>
	<br><br>
	<hr>
	MEET THE INSTRUCTORS AT BRANCH STUDIO: <br><br>
    <div class="row">
    <div class="col-sm-1"><img src="/assets/rightarrow.png"></div>
    <div class="col-sm-10">
      <div class="row">
      <!-- replace the trainers.each with paperclip link to images -->
        <div class="trainerpics"><div class="indtrainer"><% @trainers.each_with_index do |t, index| %>
            <div class="col-sm-1"><a href="/trainers"><%=image_tag("Trainer01.png") %></a>
              <div class="carousel-caption"><%= link_to (t.first_name + ' ' + t.last_name), t %></div>
            </div>
        <% end %>
        </div>
        </div>
      </div>
    </div>
    <div class="col-sm-1"><img src="/assets/rightarrow.png" id="rightarrow"></div>
</div>
</div>

